package testing.tasks;

public class LoginFallidoPrimerIntento implements Task {
    private String usuario;
    private String clave;

    public LoginFallidoPrimerIntento(String usuario, String clave) {
        this.usuario = usuario;
        this.clave = clave;
    }

    public static LoginFallidoPrimerIntento con(String usuario, String clave) {
        return new LoginFallidoPrimerIntento(usuario, clave);
    }

    @Override
    public <T extends Actor> void performAs(T actor) {
        actor.attemptsTo(
                Enter.theValue(usuario).into(LoginPage.TXT_USUARIO),
                Enter.theValue(clave).into(LoginPage.TXT_CLAVE),
                Click.on(LoginPage.BTN_INGRESAR)
        );

        actor.should(
                seeThat(WebElementQuestion.the(LoginPage.MENSAJE_ERROR_1),
                        WebElementStateMatchers.isVisible())
        );

        actor.attemptsTo(Click.on(LoginPage.BTN_ACEPTAR_ERROR_1));
    }
}

